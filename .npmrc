# Speed up npm install
prefer-offline=true
audit=false
fund=false
progress=false

# Use legacy peer deps to avoid conflicts
legacy-peer-deps=true

# Disable optional dependencies that may fail
optional=false
